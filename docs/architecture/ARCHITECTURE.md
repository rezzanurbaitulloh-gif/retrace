# RETRACE Architecture
See README and PRD for full blueprint. Key: Mobile (Flutter/Dart) + Web (Next.js) + Supabase (Postgres, Realtime, RLS). Offline-first queue, provider-agnostic maps, honest platform limits.
